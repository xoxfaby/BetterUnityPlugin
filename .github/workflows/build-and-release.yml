name: Build and Release

on:
  push:
    tags:        
      - 'v*' 

jobs:
  build:
    uses: xoxfaby/actions/.github/workflows/dotnet.yml

  release:
    uses: xoxfaby/actions/.github/workflows/nuget.yml